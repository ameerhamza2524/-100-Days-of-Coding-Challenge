// Making a list (enum) for different types of forots
enum forotsType {
    apple,
    mango,
    orenge
}

// Showing one type of forotstype from the list
console.log(forotsType.mango); // It shows 0 because enums start counting from 0
// Here, we're just checking what number the apple category got in our list.